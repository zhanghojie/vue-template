// 按需全局引入 vant组件
import Vue from 'vue'
import { Button, Cell, CellGroup, Divider, Icon, List, NavBar, Progress, } from 'vant'
Vue.use(Button).use(NavBar).use(Progress).use(Icon).use(Divider).use(List).use(CellGroup).use(Cell)
